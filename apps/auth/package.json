{
	"name": "auth",
	"version": "1.0.0",
	"description": "The authentication service",
	"main": "./src/index.ts",
	"scripts": {
		"start": "pm2-runtime ./src/index.ts",
		"dev": "pm2-dev ./src/index.ts",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"dependencies": {
		"ult-constants": "*",
		"ult-utils": "*"
	},
	"devDependencies": {
		"tsconfig": "*",
		"eslint-config-custom": "*"
	}
}
